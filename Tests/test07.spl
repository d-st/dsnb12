  // Das ist ein Kommentar.

type myInt = int;
  type vector = array [5] of int;
  type matrix = array [3] of vector;
  type mat3 = array [10] of array [20] of vector;

//Beispiel fuer gleiche Typen:
  type typ1 = array [5] of int;
  type typ2 = typ1;

//Beispiel fuer verschiedene Typen:
  type typ3 = array [5] of int;
  type typ4 = array [5] of int;
proc main() {
  var x: int; // llalalala 
  var y: int;
  var das_ist_ein_Bezeichner: int;
  var v:vector;
  var a: int; var b: int;

var i: int; var j: int; var k: int;

  x := -1;// x:=1
  x:= 1234;
  x:=  0x1a2f3F4e;
  x:='a';
  y:=x+'\n';

   v[3] := v[2] * 2;

if (x < 0) x := 42;
  if (x < 0) x := 42; else x := 43;
x:=0;
while (x < 10) x := x + 1;

 { k := i; i := j; j := k; }

  swap(x, y);
  sum(3 * x + 5, 9 - y / 2);

  x:=1;y:=1;
  b:=x;
  b:=-x;
  a:=3 + x * y;
  x:=(3 + x) * y;
  x:=5 * v[y-2];
  if(i < y);
  if(b - 2 # a + 3);else;
  if(b - 2 # a + 3);else {}
  if(b - 2 # a + 3);else {;}
  if(b - 2 # a + 3);else {;;}
  if(b - 2 # a + 3){}else {}
  if(b - 2 # a + 3){;}else;
}

// Beispiele prozedurdeklarationen
  proc nothing() {}
  proc copy(i: int, ref j: int) { j := i; }
  proc swap(ref i: int, ref j: int) {
    var k: int;
    k := i; i := j; j := k;
  }

proc sum(a:int, b:int) {
  printi(a+b);
  printc('\n');
}


